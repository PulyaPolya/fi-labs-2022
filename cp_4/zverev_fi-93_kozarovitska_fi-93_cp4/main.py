import scipy.stats as sps
import math
import vectors as v
import Geffe as g

# f =  open('z')
# arr_z_t = list(f)
# arr_z = [int(x) for x in arr_z_t[0]]
# # arr_z = []
# # for elem in arr_z_t[0]:
# #     arr_z.append(int(elem))
# print(arr_z)
l1 = g.L1(5)
l1.set_key([1,1,0,1,0])
l1.generate()
